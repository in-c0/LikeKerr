# Demo data

Do not commit copyrighted match video by default.

For the first reproducible vertical slice, use tracking data whose public use is permitted and convert it into a small derived player-trace fixture.

Suggested fixture fields:

```csv
t_ms,x_m,y_m,speed_mps,accel_mps2,effort_0_1
```
