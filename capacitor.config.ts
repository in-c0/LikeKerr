import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "studio.lifeos.likekerr",
  appName: "LikeKerr BLE Diagnostics",
  webDir: "apps/mobile/native/dist",
};

export default config;
